# Redis 连接配置（默认适配 docker-compose 中的 Redis 服务）
REDIS_HOST=localhost          # 若 Redis 在本地独立运行，可改为 localhost
REDIS_PORT=6379
REDIS_PASSWORD=           # 若 Redis 有密码，需填写

# 应用服务配置
PORT=3000                 # 服务监听端口（确保不与其他服务冲突）
NODE_ENV=development      # 环境类型（development/production）

# 日志配置
LOG_LEVEL=info            # 日志级别（debug/info/warn/error）

# Playwright 配置
PLAYWRIGHT_HEADLESS=true  # 是否无头模式运行浏览器（开发时可设为 false 查看界面）